[Unit]
Description=Trash Heatmap Node App
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/opt/trash_heatmap
EnvironmentFile=/etc/trash_heatmap.env
ExecStart=/usr/bin/node server/server.js
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
